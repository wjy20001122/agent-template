# Basic Example

这是一个最小示例，展示如何使用 `template/` 生成一个 Agent 友好的项目结构。

建议流程：

```bash
bash scripts/create-project.sh my-basic-project
cd my-basic-project
bash init.sh
```

然后填写：

- `AGENTS.md`
- `CLAUDE.md`
- `docs/requirements.md`
- `.harness/state/feature_list.json`
